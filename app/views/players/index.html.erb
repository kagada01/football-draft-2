<h1>All Players</h1>

<% @players.each do |player| %>

  <div>
  	<h3><%= link_to player.name, player_path(player) %></h3>
    <h4><%= player.position %> | <%= player.college %> | <%= player.nfl_team %></h4>
  </div>
  <%= link_to "Draft", draft_path(player), method: :patch %>
<% end %>
